# FileScope V5 クイックスタート

## 起動

`FileScope.exe` をダブルクリックします（インストール作業はありません）。
Windows SmartScreenの警告が出た場合は「詳細情報」→「実行」を選んでください（署名なしの社内配布版のため）。

## 3手で検索

1. **検索場所** … 探したいフォルダを「参照」で選ぶ（`;` で区切ると複数フォルダを同時に検索できます）
2. **検索** … 語を入力（例: `耐久`）
3. **検索開始**

結果は1ファイル1行です。行を選ぶと右側に根拠（セル番地・ページ・段落）が出ます。

## すぐ使える検索式

| 入力 | 意味 |
| --- | --- |
| `AAA BBB` | OR（「詳細設定」でANDに変更可） |
| `AAA & BBB` | 同じファイルに両方（別シート・別ページでも可） |
| `AAA, BBB` | どちらか |
| `!AAA` | AAAを含まない |
| `"耐久 試験"` | 完全一致フレーズ |
| `2of(AAA,BBB,CCC)` | 3つ中2つ以上 |
| `NEAR(電源,ノイズ,100)` | 同じページ/セル内で100文字以内 |
| `type:pdf 耐久` | PDFだけを対象に検索 |

覚えなくても「条件」ボタンから同じ条件を作れます。

## 検索モード

- **高速** … 索引中心。オンライン専用ファイルは取りに行きません
- **標準**（推奨） … 索引済みは索引、変更/未索引だけ本文を読みます
- **完全** … 索引を信用せず本文を確認します（時間はかかりますが最も確実）

## OCR（スキャンPDF）

Tesseract OCRがインストールされていれば、スキャンPDFも検索できます。
FileScopeは `%LOCALAPPDATA%\Tesseract-OCR\tesseract.exe` などを自動で探します。
未導入でも通常のPDF・Office・テキスト検索は問題なく使えます（画面右上に `OCR: 未導入` と出ます）。

## 覚えておくと便利

- `Ctrl+L` 検索欄 / `Ctrl+S` CSV保存 / `F3`・`Shift+F3` 次の一致 / `Ctrl+B` 条件ビルダー
- 結果を右クリック → 開く / フォルダを開く / Excelの該当セル / コピー / 確認済み
- 「結果をさらに絞る」で、検索し直さずに結果を絞り込めます
- 「診断」で環境情報をコピーできます（問い合わせ時に添付してください）

## 困ったとき

```powershell
:: 環境と機能のセルフテスト（ユーザー文書は読みません）
.\FileScope.exe --self-test

:: 診断情報の表示
.\FileScope.exe --diagnostics
```

結果が0件のときは、画面上部のカバレッジ（発見／検索完了／未検索／エラー）と
下部の「問題 / 未検索」を確認してください。読めなかった資料は理由付きで表示されます。

詳しくは `README.md`、実機確認は `docs\LOCAL_ACCEPTANCE_TEST.md` を参照してください。
